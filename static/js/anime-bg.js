document.addEventListener('DOMContentLoaded',()=>{const hero=document.querySelector('.hero');if(!hero)return;const reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const fine=window.matchMedia('(pointer:fine)').matches;if(reduced||!fine)return;let tx=0,ty=0,cx=0,cy=0,raf;const render=()=>{cx+=(tx-cx)*.06;cy+=(ty-cy)*.06;hero.style.setProperty('--px',`${cx}px`);hero.style.setProperty('--py',`${cy}px`);raf=requestAnimationFrame(render)};hero.addEventListener('pointermove',e=>{const r=hero.getBoundingClientRect();tx=((e.clientX-r.left)/r.width-.5)*28;ty=((e.clientY-r.top)/r.height-.5)*18},{passive:true});hero.addEventListener('pointerleave',()=>{tx=0;ty=0});raf=requestAnimationFrame(render);window.addEventListener('pagehide',()=>cancelAnimationFrame(raf),{once:true})});